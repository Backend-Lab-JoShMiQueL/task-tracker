export enum TaskStatus {
	Todo = "todo",
	InProgress = "in-progress",
	Done = "done",
}

export enum Command {
	Add = "add",
	Update = "update",
	Delete = "delete",
	List = "list",
	SetStatus = "status",
}
